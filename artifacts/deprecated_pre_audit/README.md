# Deprecated pre-audit artifacts

These files were produced by experiment implementations invalidated by the
June 2026 audit. They are retained only for provenance and must not be used as
evidence.

- E1 measured SAE error on a fresh unsteered pass.
- E2 selected signed crossings incorrectly and filtered prompts with its outcome proxy.
- E4 mixed normalized and raw coordinates and used a delayed controller state.

Repaired experiments write filenames containing `post_audit`.
